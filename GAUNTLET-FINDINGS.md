# GAUNTLET LOOP v2 — Findings Register

Improvement opportunities logged by the 15 worker runs and 15 blind-critic runs during the 2026-08-08 polish gauntlet — things found but deliberately **not** implemented (out of polish scope, needs contract changes, or needs Todd's call). 225 raw findings, deduped to ~200, ranked by impact.

Piece tags: A onboarding · B settings · C pill/HUD · D tray · E dev-first engine · F landing page.

## Distribution — the "damaged" dialog (2026-08-17, branch `feat-brew-cask`)

The "FunButton is damaged and can't be opened" dialog has hit Todd/testers **four**
times (2026-08-02, 08-14, 08-15, 08-17 on v0.1.8). Every prior answer was
`xattr -cr` — a conversion killer that trains users to bypass macOS security. This
branch ships a certificate-free mitigation and, in doing so, corrected a wrong
premise and found the real root cause.

**Root cause (verified, not assumed):** it is NOT "unsigned + quarantine" alone —
it's a **structurally invalid ad-hoc signature** plus quarantine. On this machine
`spctl -a -t exec` / `codesign --verify` report *"code has no resources but
signature indicates they must be present"* (the bundle's resource seal is broken:
`Sealed Resources=none`). A quarantined app that fails signature validation is
what macOS labels "damaged." Remove quarantine (any path) → it launches; the
broken seal alone is cosmetic on Apple Silicon once not quarantined.

**Premise corrected:** `brew install --cask` does **not** strip quarantine by
default. On Homebrew 6.0.17 (default `HOMEBREW_CASK_OPTS` unset) a plain cask
install left `com.apple.quarantine` (value `0381;…;;…`) on the app. The two OSS
competitors (freeflow, unramble) ship plain casks that "just work" only because
their bundles are validly signed — ours isn't. So the cask must strip quarantine
explicitly (`postflight`), which it now does; the curl installer does it inline.

**Shipped fix:** tap `todddickerson/homebrew-funbutton` (`brew install --cask
todddickerson/funbutton/funbutton`), `scripts/install.sh` (`curl … | bash`),
`scripts/update-cask.sh` (release sync), landing reorder (brew → curl → manual).
Proven end-to-end: post-install `xattr` shows no quarantine, app launches, engines
up; uninstall + zap verified.

**Residual / follow-up findings:**
- **[dist] The manual .dmg path is still exposed.** Browser-stamped quarantine +
  broken seal = "damaged" for anyone who downloads the .dmg directly. Only a real
  Developer ID signature + notarization closes it (SIGNING.md). Brew/curl users are
  covered; direct-download users are not.
- **[dist] Fix the ad-hoc signature seal even before Developer ID.** The bundle
  ships with `Sealed Resources=none` (linker-signed, no `_CodeSignature`). A proper
  ad-hoc `codesign --deep -s -` in the build would make even a quarantined app read
  as "unverified developer" (right-click-Open) rather than "damaged" — a strictly
  better failure mode for the manual path. Root-cause build fix, out of scope here.
- **[dist] `brew audit --cask --new` can't fully pass while unsigned.** Two checks
  fail inherently: repo "not notable enough" (homebrew-core submission rule, N/A for
  a third-party tap) and "signature verification failed" (needs notarization). Plain
  `brew audit --cask` is clean.
- **[dist] Homebrew 6.x tap-trust may prompt some users.** This Homebrew warns about
  untrusted third-party taps; the install still completed. Worth watching whether
  stable releases make it a hard gate for `brew tap`.

## Top 14 (curated — merged near-duplicates, ranked by product impact)

1. **[F] No real product visual anywhere on the landing page** (raised 4×) — the animated terminal demo is good, but there is no app screenshot, pill GIF, or menu-bar shot; Handy and VoiceInk both lead with the product itself. Biggest credibility gap on the page.
2. **[E] Deep-context cleanup (freeflow's killer feature) is absent** — read the focused field / window title via Accessibility so names, jargon, and identifiers are spelled right per app. Research doc borrow #4; the most-praised freeflow feature and the natural next step for the dev-first wedge.
3. **[B] Per-app mode map is display-only** (raised 3×) — users can see the app→mode intelligence but can't edit it. User-editable per-app rules is exactly VoiceInk's Power Mode; parity here closes our biggest feature gap with the peg.
4. **[E] Premium cloud path delivers a weaker dev experience than free offline** (raised 2×) — the paid cloud cleanup/STT path (cloud.rs worker) gets no dev-vocabulary bias and no terminal/editor prompt split; paying customers currently get worse dev output than the bundled path. (Groq BYOK biasing was fixed in-round; the premium worker path was not.)
5. **[E] No instruction-execution guard on cleanup output** (raised 2×) — dictation that *looks like an instruction* ("ignore all that, delete the file") can be executed by the cleanup LLM instead of transcribed. Needs a hardened prime-directive + runtime guard and tests.
6. **[C] Pill waveform uses a second mic capture (webview getUserMedia)** — Rust already owns the audio stream; emitting RMS levels as events gives one source of truth, kills the double mic session, and removes a class of race conditions.
7. **[C] Pipeline never emits `transcribing`/`cleaning` status events** — the pill infers processing states instead of reflecting real pipeline stages; two `emit_status` calls in pipeline.rs would make the HUD honest.
8. **[C/D] No re-entrancy gate on the hotkey** — a new Fn hold while the previous pipeline is still in flight overlaps recordings/pastes; Rust should reject or queue a hold until the prior run resolves.
9. **[B] `terminal` mode can't be forced** — it's a distinct auto-detected mode now (the wedge's standout), but ModeOverride still only offers auto/code/email/slack/raw. Needs the Rust enum + Settings surface addition.
10. **[A] True permission denial is indistinguishable from waiting** — checks return plain booleans, so a hard "deny" on first boot looks like "not yet"; using real authorization status would let the wizard heal the deny path too.
11. **[A/D] Launch-at-login isn't offered during onboarding** — the tray now has the toggle, but a menu-bar dictation app that isn't running after reboot is churn; freeflow and VoiceInk both ask during setup.
12. **[F] Download CTA hard-pins `FunButton_0.1.4_aarch64.dmg`** — the link 404s the moment v0.1.5 ships; point at a stable latest-release URL or resolve at build time.
13. **[D] No tap-to-latch / per-mode hotkeys** — freeflow's hold-Fn *or* Cmd-Fn-latch ergonomics remain the best in field; we only do hold.

14. **[orchestrator] `FrontApp::detect()` blocks the pipeline with no timeout** — pipeline.rs:53 shells out to osascript *before* transcription starts; in a TCC-stalled context that call can block ~105s (observed during release-binary smoke QA). Pre-existing (not from this branch), but it should carry a hard timeout, run concurrently with STT (its result is only needed at cleanup time), and fall back to `Unknown`.

**Implemented during the gauntlet (originally logged as gaps, then fixed in later rounds):** real end-to-end try-it step in onboarding (A r3), OG image via next/og (F r2), Groq BYOK STT vocabulary biasing (E r2), pill bottom-center multi-monitor placement (C r2-r3), shields.io badge removal (F r3).

**Implemented in branch `feat-context-and-modes` (2026-08-17):**
- **#2 [E] Deep-context cleanup** — new `app_context.rs` reads focused window title / element role / selected text via Accessibility (AX), off the hotkey path, bounded by `AXUIElementSetMessagingTimeout`, degrading to `None` when ungranted. Injected into the LOCAL cleanup prompt (cloud only on explicit `context_to_cloud` opt-in). Proven at runtime: window `ClickFunnels — webhooks.ts` turned `"click funnel"` → `"ClickFunnels"` on the live bundled Qwen (see RUNTIME-QA-context-and-modes.md).
- **#3 [B] Editable per-app mode map** — persisted `app_mode_overrides` store, resolution `global override > per-app rule > built-in detection`; Settings UI now edits rows, adds unlisted apps, and resets to auto. Takes effect on the next dictation, no restart.
- **#9 [B] Forceable terminal mode** — `ModeOverride::Terminal` added and surfaced in the Settings pills, the tray mode menu, and per-app rows.
- **#5 [E] guard (extended)** — added `detect_context_injection`: a window title fed into the prompt is a fresh injection surface; the guard falls back to raw when the output is hijacked by screen context. Prompt-level READ-ONLY framing is the first line, the guard the backstop.

## New findings from `feat-context-and-modes`

- **[E] Selected-text context can leak the wrong tone in long selections** — `AXSelectedText` is capped at 280 chars but a large highlighted block still biases vocabulary heavily; a smarter heuristic (only use selection when the focused role is a text field, or weight it below the window title) could sharpen the signal. Currently all captured fields are injected equally.
- **[E] Deep-context is not fed to the whisper STT prompt** — only the cleanup prompt gets the window title. A window like `auth_middleware.rs` could also bias the whisper initial prompt (like the dev dictionary does) so the identifier is heard right in the first place, not just spelled right after. Out of scope here (STT-prompt budget interplay), logged for a follow-up.
- **[E] `context_to_cloud` opt-in does not reach the premium worker path** — when a licensed user enables cloud context, Groq BYOK gets it but the premium worker (`cloud.rs` cleanup) still builds its own prompt server-side and never receives the context; parity requires a worker API field. Same shape as finding #4 (premium path lags free).
- **[B] Per-app rules match on app name, not bundle id** — matching is by `NSWorkspace.localizedName` (display label / raw name), so two apps with the same display name, or a localized name, could collide or miss. Keying on `bundleIdentifier` (already available from `NSRunningApplication`) would be exact; deferred to keep the UI's app names human-typable.
- **[A] Deep-context AX grant is invisible in onboarding** — the feature silently no-ops until Accessibility is granted (which onboarding already requests for paste), but nothing tells a user who turned context ON that AX is the gate; a one-line status in the Deep-context settings row (`ax_trusted()` is now exposed) would close the loop.

## Full register

### High
- **[A]** Step 7 try-it has no real end-to-end test vs freeflow's peg — freeflow's testTranscriptionStep records, transcribes, and shows the transcript in-window (idle→recording→transcribing→done state machine); FunButton only shows a mic waveform and asks for trust — a r…
- **[A]** No launch-at-login step in onboarding — freeflow and VoiceInk both offer autostart during setup; a menu-bar dictation utility that isn't running is churned — needs a new Rust command so out of scope for a polish-only round.
- **[A]** True first-boot denial is indistinguishable from waiting — check_*_permission returns a bare boolean, so a user who clicks Don't Allow on the native mic prompt still reads 'waiting' — surfacing real denied state needs a tri-state Rust command (AVAuthorization…
- **[A]** No deep-context cleanup a la freeflow PostProcessingService — freeflow feeds frontmost-app context into its cleanup prompt and it is the peg's standout brain feature; FunButton's mode-aware cleanup has no per-app context injection — product-level gap outside thi…
- **[A]** Onboarding window is 30-60px too short for step 7's worst case — 720x520 (tauri.conf.json:19-20, off-limits this round) fits steps 1-6 with margin but leaves step 7 zero headroom once the emoji-picker aside opens; 720x560 would clear it without content compression.
- **[B]** No 'terminal' option in ModeOverride despite terminal being a distinct auto-routed mode — Rust now routes terminals to a dedicated TERMINAL prompt but state.rs ModeOverride only has auto/code/email/slack/raw, so a user can never force terminal mode — needs a Rust enum addition (serde-compa…
- **[B]** Mode map is display-only — no per-app editing (VoiceInk Power Mode parity gap) — The map shows intelligence beautifully but users cannot remap an app (e.g. force Slack→raw); needs a Rust-side custom-mapping store, and dev-specific per-app profiles are the open cell the research re…
- **[B]** No user-editable per-app mode overrides (VoiceInk Power Mode gap) — The app→mode map is hardcoded in Rust app_detect.rs; VoiceInk lets users assign a mode per app, which is the exact wedge feature the Modes panel visualizes but cannot edit.
- **[B]** Scroll position leaks across tab switches — React reuses the .fb-scroll div across the settings/history/license branches (App.tsx:471, 793, 833), so opening History after scrolling Settings lands mid-list instead of at the newest entry — fix by…
- **[C]** pipeline.rs never emits funbutton:status for transcribing/cleaning — It only flips AppState.status, forcing the pill to poll get_status; emitting these events (with backend used, e.g. 'whisper · on-device' vs 'groq') would kill the poll and enable truthful engine label…
- **[C]** Move waveform level source from webview getUserMedia to Rust-emitted RMS events — The pill opens a second concurrent mic capture next to cpal's — WKWebView may show its own per-origin mic prompt on first use (or fail silently, leaving a flat dead waveform that kills the hero visual…
- **[C]** Rust does not gate a new hold while the previous pipeline is in flight — The detached pipeline task clobbers AppState.status (Transcribing/Pasting/Idle overwrite Recording), so the tray icon and get_status lie during overlapped dictations; the webview guard only patches th…
- **[D]** No per-mode global hotkeys or tap-to-latch toggle — VoiceInk gives each mode its own shortcut and freeflow's Cmd-Fn latch is the best hotkey UX in the field; FunButton is hold-only and mode switching requires the mouse (fn_hotkey.rs off-limits).
- **[E]** No runtime instruction-execution guard on cleanup output — freeflow ships appearsToHaveExecutedInstruction (PostProcessingService.swift:922-956) as defense-in-depth; FunButton relies on prompt-only protection with a 1.5B local model - the model most likely to…
- **[E]** Premium cloud STT path gets no vocabulary bias — cloud.rs CloudClient::transcribe (line 24) sends no prompt field, so paying premium users get worse dev-term transcription than free offline users — cloud.rs is outside this piece's ownership, needs i…
- **[E]** No runtime instruction-execution guard behind the prime directive — freeflow pairs its never-execute prompt with a token-overlap detector and raw-transcript fallback (PostProcessingService.swift:922-976); FunButton's bundled Qwen 1.5B will occasionally answer the dict…
- **[E]** Premium cloud path delivers a weaker dev experience than free offline — The worker's CODE_PROMPT (apps/worker/src/lib/prompts.ts:40-71) is the old shallow prompt with no DEV_DICTIONARY and no terminal semantics (terminal demotes to 'code' at pipeline.rs:172) — paying user…
- **[F]** No OG image despite summary_large_image card — layout.tsx declares a large Twitter/OG card but ships no og:image, so every share of funbutton.ai renders a bare text preview while handy.computer and tryvoiceink.com get full cards; a static 1200x630…
- **[F]** No real product visual anywhere on the page — Both pegs show the actual product (Handy: demo video; VoiceInk: app screenshots); FunButton offers only a simulated terminal animation, which is copy not proof — one real menu-bar/settings screenshot …
- **[F]** No real product evidence on the page (app screenshot / menu-bar shot / demo footage) — Critic's BAR CALL: a Handy/VoiceInk user gets no visual proof the app exists; needs a real captured screenshot or 10s recording added to apps/web/public (asset capture was outside this round's scope a…
- **[F]** No real product imagery anywhere on the page — handy.computer and tryvoiceink.com both show the actual app (menu bar, settings, recording pill); this page is 100% simulated terminal — one screenshot of the real pill/menu-bar UI or a short gif woul…
- **[F]** Download URL will 404 after next release — DMG_URL uses releases/latest/download/ with a version-pinned filename (FunButton_0.1.4_aarch64.dmg); the moment v0.1.5 is published, 'latest' points at a release that lacks that asset and the primary …
- **[F]** No real product visual on the landing page — Both pegs (handy.computer, tryvoiceink.com) show the running app; a menu-bar/pill screenshot or 10s webm near the hero is the highest-leverage addition before wider launch.

### Medium
- **[A]** Onboarding window 720x520 is ~80px too short for step 6 — Ignition panel + two provider tiles + CTA ≈ 460-480px against ~384px of stage; it scrolls gracefully but 720x580 in tauri.conf.json (off-limits this round) would fit every step scroll-free.
- **[A]** PermState "denied" and "checking" are dead states — The permission checks return plain booleans and prevDeny never yields denied/checking, so the blocked-card styling and recovery copy can never trigger proactively; real denied detection (authorization…
- **[A]** Hotkey choice is only reachable via skip escape hatches — hotkey_kind is set only by the two "use Right Option" links; external keyboards without an Fn key get no first-class "pick your key" step, while freeflow dedicates a full capture step per shortcut rol…
- **[A]** Step 1 skip jumps to engines with zero permission warning — A power user who skips lands on step 6 with mic/accessibility ungranted and won't find out until dictation silently fails; a compact permission summary chip on steps 6/7 would close the loop.
- **[A]** No "active path" readout on the engines step — With bundled models ready, Ollama up, and a valid Groq key simultaneously, nothing tells the user which backend will actually serve the next dictation; a one-line "active: bundled / ollama / groq" res…
- **[A]** Wizard progress not persisted across relaunch — Granting Input Monitoring can trigger macOS's quit-and-reopen dialog; on relaunch the wizard restarts at step 1 instead of resuming at step 5, undercutting the self-healing feel.
- **[A]** Onboarding window is too short for step 6 — geometry wish: 720x600 — Even after copy compression, step 6 (ignition panel + two option tiles + CTA) wants ~80 more vertical px; tauri.conf.json is off-limits this round so logging the wish: 720x600 would let the engines st…
- **[A]** No true 'denied' permission detection or recovery — PermState 'denied' is never set (prevDeny can't produce it), so a user who clicked 'Don't Allow' on the mic prompt sees 'waiting' forever with no 'blocked — here's how to reset it' path (tccutil reset…
- **[A]** Onboarding window height is the binding constraint — Every step now fits 424px of stage exactly, but with zero slack; 720x560 in tauri.conf.json (off-limits to me) would buy 40px of breathing room for the rescue and failed states.
- **[A]** Escape permanently dismisses the wizard — close_onboarding sets onboarded=true, so an accidental Esc on step 1 means the wizard never auto-shows again; there should be an obvious re-entry point (menu-bar item invoking the existing show comman…
- **[A]** Revocation is invisible on steps 6-7 — Permission polling is gated to steps 2-5, so a switch yanked during engine warmup or try-it never surfaces; extending the poll (or a final re-check before finish) would catch it.
- **[A]** Step-7 waveform gives no hint when mic was never granted — A user who skipped mic via the later-link path sees permanently flat bars with no explanation; a one-line 'mic not wired — go back to step 3' hint when micPerm !== granted would close the loop.
- **[A]** No aria-live announcements for auto-advance — The wizard's signature move (steps advancing themselves on grant) is silent to screen readers; an aria-live=polite region announcing 'Microphone wired — moving on' would make the healing flow accessib…
- **[A]** Esc on the hello screen permanently marks onboarding done — close_onboarding (src-tauri/src/lib.rs:222-243) sets onboarded=true even from step 1, so a reflexive Esc means a first-run user never sees the wizard again unless they discover the tray menu.
- **[A]** user-select:none on body can block selecting text in the Groq key input — onboarding.css:10 applies user-select:none globally; WebKit inherits it into inputs, hurting paste-verify workflows — scope it to non-form chrome.
- **[A]** Fixed 720x520 window leaves ~6px slack on step 7 — Step 7 now fits exactly with the aside open (424/424 measured); tauri.conf.json is off-limits so I log the geometry wish: a 720x560 window would give comfortable headroom for any future copy growth on…
- **[A]** AppleFnUsageType fix is reactive, not proactive — A Rust command reading `defaults read com.apple.HIToolbox AppleFnUsageType` could auto-open the step-7 aside (or offer a one-click fix) when the emoji-picker collision is actually present, beating dic…
- **[A]** Error status payloads are raw Rust error chains — lib.rs emits `format!("{e:#}")` on pipeline failure; the onboarding rail (and pill) render it verbatim — a short human message with detail relegated to logs would read far better at first-run.
- **[A]** No mic input-device picker anywhere in onboarding — freeflow's test step has a microphone picker (SetupView.swift:900-911) and the research doc flags mic-compat long tail as a known pain (unramble's diagnostics); FunButton records from the default devi…
- **[A]** Step 7 'enter advances' footer promise is broken by the auto-focused textarea — The landing pad autoFocuses (onboarding.tsx:729) and the keydown handler ignores INPUT/TEXTAREA targets (onboarding.tsx:136-137), so Enter inserts a newline instead of finishing until the user clicks …
- **[B]** funbutton:result payload lacks frontmost_app — The 'last: Cursor → code' routing proof has to fall back to history[0]; emitting the detected app in the result event would show routing intelligence live per dictation, the strongest VoiceInk Power-M…
- **[B]** No retry/restart action for a failed embedded engine — If llama-server or whisper spawn fails, the UI can now say 'failed' but the only remedy is relaunching the app; a restart_embedded invoke would fix the top reliability dead-end.
- **[B]** Footer version is hardcoded — It was stale at v0.1.3 while shipping v0.1.4; @tauri-apps/api/app getVersion() would make it impossible to drift again.
- **[B]** No custom hotkey capture — Only Fn and Right Option are offered; freeflow (the field's best hotkey UX per research) supports capture-your-own plus Cmd-Fn toggle latch — a real switching objection for Karabiner users.
- **[B]** History lacks per-entry delete and an instant 'wipe all now' action — The privacy story ('local-only, auto-delete') deserves an immediate wipe button; history_purge_now only enforces retention age, so a clear-all needs a new backend command.
- **[B]** validate_groq_key command exists in lib.rs but no UI calls it — The groq engine row shows 'key set' without validation; an inline check-on-blur would catch bad keys before the first failed dictation.
- **[B]** Engine rows show status but no latency telemetry — A 'last cleanup 420ms on-device' readout per engine would sell the local-speed story better than a static ready badge; pipeline already times stages.
- **[B]** History has no per-entry delete or clear-all — Retention auto-purge exists but users cannot remove a single sensitive dictation; VoiceInk offers per-entry delete.
- **[B]** Dictionary lacks import/export — Power users with big jargon lists (the dev audience) cannot bulk-load terms; a paste-CSV path already half-works via comma parsing but there is no export.
- **[B]** App->mode map is read-only; VoiceInk Power Mode is editable — The routing table shows intelligence but users cannot add an unlisted app to a mode (e.g. a niche editor stuck in auto); an 'add app to mode' affordance backed by a Rust-side override map would beat t…
- **[C]** lib.rs hides the pill BEFORE emitting pasting/error statuses — The webview must re-show itself for the flash states, causing a possible one-frame blink between cleaning and pasted; moving the Rust hide to after a flash delay (or letting the webview own hide entir…
- **[C]** Error status carries a raw anyhow chain as the user-facing message — emit_status(Error, format!("{e:#}")) can be hundreds of chars of nested context; Rust should emit a short human reason plus separate detail so the HUD and history don't have to truncate blindly.
- **[C]** Two concurrent mic captures during recording (cpal in Rust + getUserMedia in pill webview) — Works on macOS but is wasteful and could diverge on exotic devices; emitting audio-level events from the existing cpal recorder would drive the waveform from the actual recorded signal with one captur…
- **[C]** funbutton:result payload (word_count, backend) unused by the HUD — The pasted flash could show 'code · Cursor · 42 words · local' for free by also listening to funbutton:result — a stronger receipt than mode+app alone; skipped to keep the flash single-source and with…
- **[C]** Pill positions on its resident (primary) monitor, not the monitor being dictated into — monitorFromPoint(await cursorPosition()) on each recording start would make the HUD follow the user on multi-monitor rigs (Wispr does this); needs two more capability grants.
- **[C]** Native hide→show blink between cleaning spinner and pasted flash — lib.rs:810-812 hides the pill window before emitting pasting/error and pill.tsx:222 re-shows it — a native window hide/show cycle in the middle of the pill's best moment; keep the window visible throu…
- **[C]** Pill position computed once on mount, never again — pill.tsx:58-71 places bottom-center at webview startup; unplugging a display, changing resolution, or moving to another monitor strands the pill — recompute on each 'recording' status like Wispr repos…
- **[C]** Pill drag-to-reposition needs acceptFirstMouse to be real — The window is focus:false without acceptFirstMouse:true, so the first click likely never reaches the webview; a real implementation needs acceptFirstMouse in the window config + startDragging + core:w…
- **[C]** Rust pipeline never emits mid-pipeline transcribing/cleaning events — lib.rs only emits status at the pipeline edges, forcing the pill to poll get_status at 110ms during dictations; emitting real events from pipeline.rs would delete the poll loop and make stage transiti…
- **[C]** No partial-transcript streaming in the HUD — freeflow and Wispr show live words while you talk; our pill shows waveform only — streaming whisper partials into the pill would be the next signature-moment jump but requires streaming transcription …
- **[C]** Rust hides the pill mid-recording when an overlapped pipeline completes — lib.rs:810-812 hides the pill window unconditionally when any pipeline returns, even if a newer recording is live — the Rust-side companion to the pill-side overlap guard, and it also never gates Hotk…
- **[C]** Error detail dumps the full anyhow chain into an 11px one-liner — lib.rs:918 emits format!("{e:#}") — multi-clause context chains ellipsize at 244px (pill.css:164-173); mapping to short human causes ('whisper: model not loaded') would make the punk recovery copy lan…
- **[C]** visibleOnAllWorkspaces may not cover Spaces owned by fullscreen apps — Tauri maps it to NSWindowCollectionBehaviorCanJoinAllSpaces; guaranteed display over a fullscreen Cursor/terminal Space also wants .fullScreenAuxiliary on the pill NSWindow — a 3-line objc2 tweak in l…
- **[C]** funbutton:status payload should carry a structured dictation id / error kind — The overlap guard discriminates late vs same-hold errors by the "encode:" string prefix from lib.rs — a brittle cross-language contract; a {id, kind} field would make late-event detection exact and le…
- **[C]** No 'can't hear you' state during recording — The pill already computes live RMS; ~2s of near-silence while recording could flip the waveform to a low-signal hint (wrong input device, muted mic) before the user wastes a dictation — unramble/Wispr…
- **[C]** Mic teardown on release rides the 110ms status poll — Rust never emits a 'transcribing' event at the pipeline edge (only status.lock flips, pipeline.rs:71), so the webview mic stream stays hot up to ~220ms after key release (pill.tsx:220-235); one emit_s…
- **[C]** Overlapped-dictation waveform blinks once — lib.rs:810-812 hides the pill unconditionally when the previous pipeline returns and the webview re-shows it (pill.tsx:262-264), producing a visible one-frame blink mid-hold; skipping the Rust-side hi…
- **[D]** No 'Restart Local Engines' action when embedded STT/LLM fails — Tray now shows '✕ whisper — down' but the only recovery is relaunching the app; a respawn hook in embedded_stt/embedded_llm (off-limits this round) would make failure states self-serviceable.
- **[D]** Pipeline has no status-change hook, so mid-run tray/menu state can lag one phase — pipeline.rs flips Transcribing→Cleaning internally with no callback/event; tray title shows '…' throughout but the menu status line only refreshes at run boundaries.
- **[D]** No CLI flags or Unix-signal control (Handy parity gap) — Research doc calls Handy's --toggle-transcription/--start-hidden/--no-tray 'power-user catnip' for exactly the dev audience FunButton targets; FunButton has none.
- **[D]** Pipeline errors are not recorded to history — Only paste failures are stored (last_failed); a failed transcription/cleanup leaves the '!' badge with no way to inspect what happened, making the persistent error state a dead end.
- **[D]** App.tsx has no listener for funbutton:settings-changed — open Settings window shows stale mode after a tray quick-switch — tray.rs:305 emits the event but App.tsx (verified lines 182-258) never subscribes; settings only refetch on funbutton:result, so an open Settings window displays the wrong mode until the next dictatio…
- **[D]** No CLI flags / Unix-signal control (Handy's --toggle-transcription, --start-hidden, --no-tray) — The research peg ranks Handy's CLI/signal control as power-user catnip for the exact dev audience; the tray is now competitive but this adjacent affordance is still absent from the app entirely.
- **[D]** open_onboarding Tauri command has the same stale-step bug the tray just fixed — lib.rs:213 open_onboarding (invocable from frontend) shows the hidden onboarding window without reloading it, so any future frontend 'replay' button will re-show a stale step — needs the same eval(rel…
- **[D]** funbutton:settings-changed emit has no frontend listener — Tray mode switches emit the event (tray.rs) but repo-wide grep finds no listener, so an open Settings window shows a stale mode until next dictation; one useEffect listener in the settings frontend (o…
- **[D]** No tray action to restart a failed engine directly — Click-to-fix routes to Settings, but the true Handy-grade move is a 'Restart engine' item that respawns the embedded llama-server / reloads whisper in place; embedded_llm/embedded_stt expose no restar…
- **[D]** Settings window has no funbutton:settings-changed listener — tray mode switch can be silently clobbered — tray.rs:325 emits the event but App.tsx never listens; an open Settings window keeps stale mode_override and pressing Save there overwrites the tray's mode choice (save_settings replaces the whole Set…
- **[D]** Handy-peg CLI affordances still absent — The research doc's runner-up borrow (--toggle-transcription, --start-hidden, --no-tray, Unix-signal control) is Handy's actual power-user surface; no piece covers it yet and it is cheap catnip for the…
- **[E]** whisper.cpp initial-prompt truncation may invert dictionary priority — build_stt_prompt loads user terms first assuming front-of-list priority, but whisper.cpp truncates the initial prompt to its token cap internally — if it keeps the tail, an over-budget prompt silently…
- **[E]** Cloud worker code prompt now lags the desktop prompt — apps/worker/src/lib/prompts.ts still has the old generic CODE_PROMPT and no terminal mode, so paying premium users get strictly worse dev dictation than free local users — port the new CODE/TERMINAL p…
- **[E]** No deterministic post-pass for mechanical conversions — Flags, versions, localhost:port, and symbol words are regex-shaped transforms being entrusted to a 1.5B LLM; a rule-based pre/post layer would make them 100% reliable regardless of model tier and shri…
- **[E]** No runtime instruction-execution guard (freeflow has one) — freeflow's appearsToHaveExecutedInstruction() detects when the cleanup LLM answered the transcript instead of cleaning it and falls back to raw; FunButton's protection is prompt-only, and small local …
- **[E]** Terminal mode cannot see what is running inside the terminal — A shell prompt wants literal commands but Claude Code/aider inside the same terminal wants prose; reading the window title (same osascript round-trip, one extra property) would disambiguate and pick C…
- **[E]** osascript app detection costs ~50-100ms per dictation and needs Automation permission — NSWorkspace.frontmostApplication via objc2 is synchronous, permission-free, and layout-API-free (no TIS/TSM involvement), removing a per-dictation latency tax and a whole permission-prompt failure mod…
- **[E]** Cloud STT path gets no vocabulary bias — cli.transcribe (pipeline.rs:106, cloud.rs) cannot carry the STT prompt without a worker API change, which is outside this piece's file surface - premium users lose the transcription-layer half of the …
- **[E]** Terminal mode degrades to 'code' on the cloud cleanup path — pipeline.rs:167-169 maps Mode::Terminal to 'code' because the worker's mode union lacks 'terminal' - premium users dictating at a shell prompt can get sentence-cased, period-terminated commands; the w…
- **[E]** Cloud cleanup collapses Terminal mode to 'code' — pipeline.rs maps Mode::Terminal to 'code' for the cloud worker (mode union is auto|email|slack|code|raw), so cloud users lose the terminal-literal inversions (no trailing period, no sentence-casing) —…
- **[E]** No deterministic self-correction post-process fallback — Self-correction now relies entirely on the 1.5B model following the prompt; a regex pass for 'no wait / no actually / I mean' markers in post_process would guarantee the behavior when the model misses…
- **[E]** No per-app mode override (VoiceInk Power Mode gap) — VoiceInk's headline feature is per-app mode config; FunButton only has global ModeOverride plus auto-detect — a per-app override map would beat VoiceInk by combining auto-detect defaults with user pin…
- **[E]** Frontmost-app detection shells out to osascript on every dictation — app_detect.rs:31-36 spawns osascript (~50-150 ms) on the critical path before transcription; a native NSWorkspace.frontmostApplication call via objc2 is near-zero cost and shaves perceptible latency f…
- **[E]** No window-title/context-level detection for dev surfaces — Detection is app-name-only, so a GitHub PR review in a browser or an SSH session title is invisible; freeflow's deep-context (focused field + window title via Accessibility) is the next rung for the d…
- **[F]** Download URL breaks on next release — The page hardcodes /releases/latest/download/FunButton_0.1.4_aarch64.dmg — the moment v0.1.5 ships, 'latest' no longer contains that asset name and every download button 404s; needs a stable asset nam…
- **[F]** No Homebrew cask — Handy ships `brew install --cask handy` and unramble ships a tap; for a dev-first wedge, brew is the expected install path and its absence undercuts the positioning more than any page copy can compens…
- **[F]** Fn/Globe emoji-picker collision not documented — dictara's README documents the macOS Fn-key -> emoji-picker collision and the `defaults write com.apple.HIToolbox AppleFnUsageType -int 0` fix; FunButton's install block and (per the brief) onboarding…
- **[F]** Paid tiers sellable on page but checkout returns 503 — /api/checkout currently 503s (handled with a waitlist notice), yet Pro/Lifetime render as fully purchasable; a 'checkout opens soon' badge on paid cards would be more honest and reduce dead-click frus…
- **[F]** No real demo recording — The CSS terminal demo is good but every peg (Wispr, VoiceInk, freeflow) leads with actual screen capture; a 15s recording of dictating into Cursor/Claude Code would out-convert any static block.
- **[F]** chrome-devtools MCP profile is single-instance across concurrent agents — The shared chrome-profile lock made browser QA tooling unusable for this whole round while another agent held it; the orchestrator should give each worker an isolated browser profile (--isolated / dis…
- **[F]** Pro card advertises 'or $9/mo' with no monthly purchase path — page.tsx:604 displays a monthly price but the CTA only posts pro_annual, while the checkout API already whitelists pro_monthly (api/checkout/route.ts:9-16) — a monthly buyer literally cannot buy.
- **[F]** No Homebrew install path mentioned — Handy's audience installs via 'brew install --cask handy' (research doc section 1.1); even a '# brew cask: coming' line in the install block would meet the dev audience's default expectation.
- **[F]** Star badge currently reads 'stars 1' — truthful but weak social proof — Repo has ~1 star; a shields 'last commit' or 'commit activity' badge would prove liveness better than a low star count until traction arrives — worth an orchestrator decision since the critic explicit…
- **[F]** Footer claims 'no trackers on this page' but nav/success badges load from img.shields.io — Third-party image request leaks visitor IPs to shields.io; proxy the badge through an API route or cache it server-side to keep the claim airtight.
- **[F]** Hero claims 'No cloud' while Lifetime tier headlines 'Groq fast tier unlimited forever' — The default-local story is internally disclosed ('All optional. The default path never phones home.', page.tsx:349-351), but the Lifetime card leading with a cloud feature slightly muddies the page's …
- **[F]** opengraph-image.tsx not audited against new messaging — The OG image route predates the dev-first rebuild; if it still carries coming-soon copy, link previews on X/Slack undercut the new hero — outside my ownership list this round so left untouched.
- **[F]** No persistent download CTA in nav — Handy and VoiceInk keep a download affordance reachable at all scroll depths; our nav has only anchor links, so a convinced reader mid-page must scroll to hero or pricing to get the DMG.
- **[F]** Shared chrome-devtools MCP selection races between gauntlet agents — Concurrent workers overwrite each other's selected page and per-page emulation mid-QA (observed twice this round); orchestrator should give each worker its own MCP connection or a page-locking convent…
- **[F]** $149 lifetime sits 3x above the research doc's proven $25-49 band — The research doc (section 6.5) recommends the $29-49 proven band (VoiceInk $25-49, Parakeety $30, BetterDictation $39); the page presents $149 consistently, but the pricing decision itself may cap con…

### Low
- **[A]** Enter key can double-fire navigation — The global keydown handler advances on Enter while a focused button also receives the click, so Enter on a focused CTA can move two steps; checking e.target against button elements would fix it.
- **[A]** AppleFnUsageType fix is copy-paste only — A one-click "fix it for me" button running the defaults write via a Rust command would beat dictara's README-only treatment, but adds a new invoke command (contract addition).
- **[A]** Waveform renders via setState per animation frame — 28 bars re-render the React tree at ~60fps on step 7; canvas or direct style mutation would cut cost on low-end machines.
- **[A]** Grant events are not announced to VoiceOver — The stepper now has a progressbar role, but permission state flips are visual-only; an aria-live region announcing "Microphone wired" would make the auto-advance flow accessible.
- **[A]** Enter key inside the Groq input advances the wizard instead of validating — The global keydown handler (onboarding.tsx:118-129) doesn't check event.target, so pressing Enter while typing a key navigates to step 7 when stt is ready — should validate the key or be ignored in fo…
- **[A]** Footer promises 'esc closes' but no Escape handler exists — onboarding.tsx:276 advertises esc; the keydown listener handles only ArrowRight/Enter/ArrowLeft — either wire Escape to close_onboarding or drop the claim.
- **[A]** Opening the Fn/emoji aside on step 7 pushes the CTA below the fold — Measured 85px overflow when the <details> expands; tightening .ob-wave margins or collapsing the wave while the aside is open keeps 'I'm ready. Let me loose →' visible.
- **[A]** Step 6 disabled CTA next to an enabled link that does the same thing — 'waiting for an engine…' (disabled) sits beside 'I'll set this up later →' which also calls onNext — confusing affordance pair; make the escape visually secondary-but-clearly-active or gate both consi…
- **[A]** 600ms auto-advance cascade through slides 3-5 when all permissions pre-granted — A returning fully-granted user watches three slides flash by (1.8s of forced transit); consider jumping straight to step 6 with a single 'all three wired' beat on step 2 instead.
- **[A]** Ollama row ignores settings.ollama_url — The step-6 copy hardcodes 'localhost:11434' and 'qwen2.5:1.5b' rather than reading the loaded settings values a power user may have customized.
- **[A]** PermState 'checking' value is dead — Nothing ever sets 'checking' anymore; the type value and its stateLabel branch could be removed for clarity.
- **[A]** Cleared-but-previously-validated Groq key is kept on finish — validateAndSaveGroq persists immediately, so emptying the input and finishing silently retains the old key; an explicit empty-string write when the field was cleared would match user intent.
- **[A]** Defaults fallback could theoretically mask a real get_settings failure — The new never-hang fallback runs the wizard on defaults if get_settings rejects; if save later succeeds while get had transiently failed, defaults could overwrite stored settings — acceptable (all fie…
- **[A]** Step 6's disabled CTA contradicts the adjacent always-enabled skip link — 'waiting for an engine…' (disabled, onboarding.tsx:575-577) sits directly above 'I'll set this up later ->' (onboarding.tsx:578) which performs the same navigation, undermining the gate it advertises.
- **[A]** Granted-flash frame on solo permission slides is near-empty for its 600ms beat — When a slide mounts already-granted (onboarding.tsx:455-456) the viewport is a heading plus one green line in a 520px void; keeping the hero context or vertically centering the confirmation would make…
- **[A]** Status checkmarks mix a text glyph with Lucide iconography — '✓' literals (onboarding.tsx:315, 456, 488, 557, 568, 678) render font-dependent next to Lucide icons; swapping to <Check size={n}/> matches the icon system and Todd's global icon rule.
- **[A]** Step 6 'set this up later' leads to a step 7 that can only fail — Skipping with no STT engine ready means the try-it dictation errors; the rail now surfaces the error, but a targeted 'no engine yet — add one on the previous step' message would close the loop.
- **[A]** Echoed transcript doesn't scroll to match real-paste caret position — The 700ms fallback echo sets textarea.value leaving scrollTop 0 (reads from the start), while a real paste leaves the caret at the end — harmless inconsistency but worth unifying if transcripts get lo…
- **[A]** Stepper step names invisible to VoiceOver — The stepper exposes role=progressbar with numeric value only; per-node step names ('mic', 'engines'…) are visual-only, so screen-reader users hear position without meaning.
- **[A]** Pipeline rail hardcodes 'qwen' even when cleanup runs on cloud fallback — PIPE_STAGES and railMsg say 'qwen'/'qwen scrubbing…' (onboarding.tsx:597, 706) while the result payload can report backend 'cloud-fallback' — label the stage 'cleanup' or derive it from the backend to…
- **[A]** Permission slides leave the lower ~40% of the stage empty before the 8s rescue appears — Steps 3-5 top-align in .ob-slide.compact (onboarding.css:143) so the first 8 seconds show a large dead zone under the LiveScan pill; vertically centering the hero+CTA cluster or reserving the rescue's…
- **[A]** Step 6 disabled CTA reads semi-active — 'waiting for an engine…' renders as 45%-opacity brand pink (onboarding.css:189) which still reads clickable at a glance — a ghost/neutral disabled treatment would telegraph the gate more clearly.
- **[A]** Step 7 opens its own getUserMedia/AudioContext while the Rust core also captures the mic — The level meter (onboarding.tsx:619-652) runs a second concurrent capture during the real Fn recording; macOS tolerates shared mic access but reusing a level event from the core would remove the dupli…
- **[A]** Step 7 stage overflows by exactly 1px with the emoji-picker aside expanded — Measured 521px content in a 520px stage (overflow:auto hides it); shaving 1px from .ob-slide.go gap or the aside padding makes the fixed-window budget exact.
- **[A]** Geometry wish: none — 720x520 holds — All seven steps including worst-case rescue/failure/disclosure states fit the fixed window; no tauri.conf.json change is needed for this surface.
- **[B]** Groq key has no inline validation — Key saves on blur but the user only learns it's bad on first dictation; freeflow validates the key during setup with a live check.
- **[B]** Fn/Globe emoji-picker collision not surfaced — dictara documents the AppleFnUsageType fix as a must-solve Fn-app papercut (research §1.5); onboarding/Button section should detect or at least mention it.
- **[B]** History lacks per-entry delete and clear-all — A privacy-positioned local-only app should let users purge a single embarrassing entry without waiting for retention expiry.
- **[B]** Dictionary is preserve-spelling only, no find→replace pairs — VoiceInk ships text replacements alongside its dictionary; 'gh pr' → 'gh pr' style expansions are a cheap dev-first win.
- **[B]** words_today is the only usage metric — A tiny weekly/streak readout in the stats row would sell the daily habit the way Wispr's dashboard does, at near-zero cost.
- **[B]** Pill toggle groups lack aria-pressed / radiogroup semantics — Screen-reader and keyboard users get no selected-state announcement on hotkey/backend/mode/retention pills.
- **[B]** fb-label-aux lowercases app names in the routing trace — text-transform: lowercase (App.css:169) renders 'last: cursor → code'; preserving app-name case would read more precise for a dev audience.
- **[B]** Status glyphs (●◐○✕) are text characters rather than Lucide icons — Coherent with the mono-terminal aesthetic, but glyph metrics vary across fonts/platforms; Todd's global standard prefers Lucide for system UI if consistency ever wobbles.
- **[B]** Ollama status only refreshes on mount/manual recheck — If the user starts Ollama while the window is open the row stays 'not running' until they click recheck; a focus-triggered or 30s poll would fix it.
- **[B]** Header words-today can go stale across midnight — Counter re-reads settings only on dictation results; a window left open past the words_today_date rollover shows yesterday's count until the next dictation.
- **[B]** Welcome dismissal is WebView-localStorage only — A reinstall or WebView data reset re-shows the intro; persisting to a Settings field would survive, but settings.onboarded is owned by the wizard flow so I did not overload it.
- **[B]** Status message overflow in 540px header — Long funbutton:status messages concatenate into the header label with no ellipsis clamp; a max-width + text-overflow on .fb-status-label would protect the new words counter from being pushed out.
- **[B]** History search invokes history_list on every keystroke — App.tsx:273-276 re-queries SQLite per character with no debounce; harmless locally but a ~150ms debounce would be tidier.
- **[B]** Unicode status glyphs instead of Lucide icons — EngineRow/PermRow use ●/◐/✕/○ text glyphs (App.tsx:935, 1229); consistent with the terminal aesthetic and not a slop violation, but Todd's global rules prefer Lucide for system UI — a swap with aria l…
- **[B]** fb-label-aux lowercases live app names — text-transform: lowercase (App.css:184) turns 'Cursor' into 'cursor' in the 'last: cursor -> code' label; deliberate style but it distorts app identity — consider exempting the app-name span.
- **[B]** Toast palette hardcoded regardless of theme — App.css:794 pins toasts to #1a1a1a/#f0f0f0; the macOS-style always-dark toast reads fine in both themes but is the only non-token color surface in the file.
- **[B]** Concurrent-session note: shared Chrome instance contention — Another gauntlet piece's dev server (port 5199) repeatedly navigated the shared Chrome MCP pages during visual verification; not a code issue, but future rounds should use isolated browser contexts fr…
- **[C]** HUD copy cannot name the actual hotkey — get_hotkey_label invoke exists but returns a long Settings-page string; a short machine-readable hotkey kind would let error/hint copy say 'hold fn' vs 'hold right option' instead of the brand-agnosti…
- **[C]** User-dragged pill position resets to bottom-center on every app restart — Position is recomputed on webview mount with no persisted preference; a pill_position field in Settings would respect deliberate placement.
- **[C]** Pill webview is resident from app launch for a HUD visible seconds per day — A WKWebView holds tens of MB; lazy window creation on first dictation (or a native NSPanel HUD long-term) would cut idle footprint — relevant vs VoiceInk/unramble native Swift HUDs.
- **[C]** No 'canceled' affordance while recording — Competitors (Wispr, freeflow) support press-Esc-to-discard mid-recording; the pill has no state for it because the Rust hotkey loop has no cancel path — a real UX gap for dev users who fat-finger the …
- **[C]** Fast pipelines can skip the 'cleaning' stage display entirely — The 110ms poll (pill.tsx:200) can miss a sub-100ms cleanup step; having pipeline.rs emit transcribing/cleaning statuses directly would make stage progression deterministic instead of sampled.
- **[C]** Fixed 244px capsule across all states — no width morph — Wispr's signature move is the capsule animating width between recording and the result flash; a CSS width transition keyed off the phase class would push this from 'competitive' to 'screen-record it f…
- **[C]** Recording state shows no elapsed-time or silence feedback — A subtle duration counter or a low-input treatment (dimmer bars when RMS stays near zero) would catch dead-mic situations before the user releases and gets a transcription error — VoiceInk surfaces th…
- **[C]** Visible pill window intercepts clicks across its full 300x96 bounds — The transparent regions around the capsule swallow mouse events while the pill is shown; Tauri's set_ignore_cursor_events would make it click-through like a true HUD.
- **[C]** Pill position set once on mount, stale after display changes — Bottom-center placement runs once when the webview mounts; unplugging a monitor or changing resolution mid-session leaves the pill at stale coordinates until restart — re-running placement on each sho…
- **[C]** Error state hint is generic across failure types — The detail line shows the raw error but the recovery hint is always 'hold the button · run it back'; distinguishing no-model / network / accessibility-permission failures with targeted hints would cut…
- **[C]** Mic analyser pattern duplicated between onboarding Step7 and pill — Both implement getUserMedia + AnalyserNode + RAF teardown independently; a shared useMicLevel hook would keep the teardown/race discipline in one place.
- **[C]** Pasted receipt should prioritize the app name over the 'pasted' tag when tight — Long app names truncate ('prose mode · Visua…') while the static tag keeps space (pill.css:144-160); drop the tag under overflow or let capsule width grow per state like Wispr's pill.
- **[C]** Capsule border-color state changes snap instead of transitioning — pill.css:21-56 tints the border per state but .pill declares no border-color transition; a 0.2s ease would smooth recording→transcribing→flash handoffs.
- **[C]** pill.json carries three redundant permissions — core:window:allow-outer-size, allow-current-monitor, allow-primary-monitor are already granted to the pill via core:window:default in default.json (verified in gen/schemas/desktop-schema.json); trimmi…
- **[C]** Waveform could be fed from the Rust recorder instead of a second mic capture — The webview opens its own getUserMedia stream alongside cpal's capture; emitting level samples from the Rust recorder would guarantee the waveform reflects exactly what is being recorded and drop the …
- **[C]** Overlay style preference (compact vs full) is a cheap parity win vs freeflow — reference/freeflow-swift/SetupView.swift:108 ships a use_compact_overlay setting; a HUD size/position preference is a low-cost settings nicety for the dev audience.
- **[C]** Same-hold 'audio:' start-failure error can be swallowed in a ~110ms race — If recorder.start() fails within the window where the pill still shows the previous hold's recording state, the guard eats the flash; state self-heals via the get_status poll but the error is invisibl…
- **[C]** One-frame stale-position flash when switching monitors — Rust shows the pill window before the webview's place() resolves, so the first frame can paint at the previous monitor's position; Rust positioning before show() (or show-after-ack) would remove it.
- **[C]** Waveform history bars could fade with age — Wispr's pill fades older waveform samples; a per-bar opacity ramp (3 lines of CSS) would add depth — deliberately not touched in round 3 since the critic already passed the waveform.
- **[C]** Receipt truncates long app names despite CSS comment claiming otherwise — 'prose mode · Visual Studio Code' renders as 'Visual Stud…' at the 284px stretch (verified by screenshot), contradicting pill.css:62-64; shorten known app names Rust-side (e.g. 'VS Code') where the pa…
- **[C]** Placement race on monitor switch — place() runs async in the webview after Rust's p.show() (pill.tsx:74-99 vs lib.rs:746-748), so on a monitor change the pill can flash at its stale position for a frame; positioning Rust-side before sh…
- **[C]** pill.json carries redundant permissions — outer-size, current-monitor, primary-monitor, cursor-position, and monitor-from-point are already granted to the pill window via core:window:default in default.json (verified against gen/schemas/deskt…
- **[C]** Error detail is a raw anyhow chain with an unreachable tooltip — lib.rs:918 emits format!("{e:#}") which can be long and technical for the 284px err-main line, and the title tooltip (pill.tsx:371) is unreachable on a focus:false window that auto-dismisses in 5.2s; …
- **[D]** words_today only rolls over on the next dictation — The tray footer can show yesterday's count before the first dictation of a new day; fixing it from the tray's read path would make snapshot() side-effecting, so it belongs in a daily timer or the sett…
- **[D]** Copy Last Dictation gives no visual confirmation — Menu closes silently on click (native behavior, matches Handy); a brief '✓' set_title flash would confirm the copy without a notification — worth a taste pass.
- **[D]** Tray icon is the reused app icon, not a purpose-drawn 18pt template glyph — A dedicated menu-bar mark (e.g. a rounded button outline that fills while recording via set_icon swap) would out-polish the text-dot approach; deferred since icon design is a brand call and assets are…
- **[D]** STT health line doesn't distinguish BYOK Groq vs licensed premium cloud fallback — 'down, using cloud' is shown for both; naming the actual fallback would help debugging trust in the offline promise.
- **[D]** Autostart toggle failures are log-only — If the launchd plist write fails the check item silently reverts on next sync with no user-visible explanation.
- **[D]** Tray menu status line lags one phase during Cleaning/Pasting — pipeline.rs:71,162 set Status::Transcribing/Cleaning without a tray::sync, so a menu opened mid-pipeline shows '◌ transcribing…' while cleaning; the menu-bar title '…' is identical across phases so on…
- **[D]** Error state '!' in the menu bar persists until the next successful run with no detail affordance — tray.rs:92-97 documents this as deliberate, but a permanent '!' with only 'last run failed — try again' (tray.rs:338) and no 'show error details' path undersells the diagnostics the app already logs; …
- **[D]** Copy Last Dictation could carry a relative timestamp alongside the word count — 'Copy Last Dictation (42 words)' (tray.rs:204-213) is good; '42 words · 2 min ago' is the VoiceInk/Wispr recent-item polish level and the data already exists in history.
- **[D]** Failed-engine click cannot deep-link to the engine section of Settings — No event/anchor contract exists for settings tabs (only funbutton:open-history), so the click lands on the settings window generically; a funbutton:open-engines event plus frontend listener would fini…
- **[D]** Open-menu status line lags one phase during Cleaning — pipeline.rs:162 sets Status::Cleaning without a tray::sync call, so a menu opened mid-pipeline shows 'transcribing…' during cleanup; one sync call in pipeline.rs (off-limits to this piece) fixes it.
- **[D]** Possible one-frame flash of stale onboarding step on replay — eval(reload) then show is asynchronous, so the old DOM may paint briefly before the reload blanks it; gating show on the webview's load event would make the restart visually seamless.
- **[D]** Onboarding finish() never resets React step state — onboarding.tsx:186-192 persists the final step in component state forever; the Rust-side reload papers over it, but a frontend-owned reset on funbutton:onboarding-complete would fix the root cause.
- **[D]** words_today footer does not roll over at midnight — tray.rs:131-139 snapshot reads s.words_today without checking words_today_date, so the footer shows yesterday's count until the next dictation resets it.
- **[D]** Menu status line lags during Cleaning/Pasting pipeline phases — pipeline.rs internal transitions (off-limits to this piece) never call tray::sync, so an open menu can say 'transcribing…' during cleanup; the title glyph is the same '…' for all three states so the m…
- **[D]** No 'Paste Last (⌘⇧V)' menu item despite the shortcut existing — The tooltip advertises ⌘⇧V re-paste (tray.rs:393) but the menu only offers Copy Last; surfacing the paste action as a menu item would make the shortcut discoverable.
- **[D]** Recording state uses a title glyph rather than an icon swap — A dedicated recording template icon (additive asset was allowed) would read louder at a glance, though the ● title next to the template icon is an acceptable Wispr-style treatment.
- **[E]** No per-app mode overrides (VoiceInk Power Mode gap) — Settings schema was locked this round; a per-app map (e.g. 'Ghostty → raw', 'Slack → email') is the obvious next differentiator once the schema can change.
- **[E]** STT prompt budget could rise from 600 chars toward the real 224-token cap — With the STT list now curated to 527 chars there is headroom, but a token-aware (not char-aware) budget would let large user dictionaries and the dev list coexist without blind truncation.
- **[E]** Dictionary has no user-visible surface — Competitors market their vocab (VoiceInk 'custom vocabulary', freeflow 'context awareness'); showing the built-in dev dictionary (read-only) in Settings would make the dev-first wedge visible instead …
- **[E]** DEV_DICTIONARY injection into the cleanup prompt is untruncated — pipeline.rs:148 appends all ~150 DEV_DICTIONARY terms plus the full user dictionary to every dev-mode cleanup prompt; fine for the bundled model's context but worth a budget ceiling as the dictionary …
- **[E]** Browser-based dev surfaces are unclassified — github.com PR reviews, Colab, StackBlitz and similar in Safari/Chrome fall to FrontApp::Other -> Mode::Auto (app_detect.rs:81); freeflow reads AX context for this - URL/tab-title sniffing would extend…
- **[E]** Terminal vim is invisible to app detection — Only GUI vim (MacVim) hits FrontApp::Vim (app_detect.rs:59); vim inside a terminal gets TERMINAL_PROMPT whose no-prose rules are wrong for comment/doc editing - inherent to process-name detection, wor…
- **[E]** STT prompt budget is chars, not tokens — build_stt_prompt truncates at 600 chars as a ~224-token proxy; a user dictionary of long/multibyte terms could overshoot the real token cap — a proper tokenizer count (or whisper-side truncation check…
- **[E]** Comma-list STT prompt vs sentence-form bias — Whisper initial-prompt biasing reportedly works better as natural-language context than a comma list; worth an A/B against a sentence-form prompt ('A developer discussing git, kubectl, ...').
- **[E]** Offline STT proof test permanently ignored in CI — embedded_stt::tests::transcribes_real_wav_offline is #[ignore]-gated on the model file, so the canonical proof phrase is never exercised end-to-end in CI — a cached-model CI job would close this.
- **[E]** AI chat desktop apps classified as generic Other — ChatGPT/Claude desktop apps fall to Mode::Auto via FrontApp::Other; they are prose-with-dev-vocab surfaces where the dev dictionary (but not symbol substitution) would help — a fourth internal surface…
- **[E]** CODE_PROMPT teaches doubled backslash — cleanup.rs:173 renders as 'backslash' → \\ (two chars) because the source escapes twice ('\\\\'); change to '\\' so the model emits a single backslash — verified by compiling the literal.
- **[E]** build_stt_prompt has no unit test for user-first ordering and truncation — The user-dictionary-outranks-built-ins guarantee (pipeline.rs:258-281) is documented and load-bearing but untested; a small test with an oversized user dictionary would pin it.
- **[F]** app/showdown page exists but is orphaned — A /showdown route builds and deploys but nothing links to it; either link it from the comparison section (if it's the interactive comparison) or gate it before it's discovered half-finished.
- **[F]** Comparison-table facts will drift silently — Competitor pricing/claims are hardcoded from the 2026-08-01 research snapshot; worth a dated 'as of' footnote or a build-time check so the honest table stays honest.
- **[F]** Default Next favicon still shipped — The brand is literally a red button glyph but the tab icon is the create-next-app default; a favicon matching .fb-glyph is a cheap polish win (public/ was outside this piece's ownership).
- **[F]** Dingbat checkmark in email success state — page.tsx:506 uses a raw '✓' character; per the user's global UI rules, system UI glyphs should be Lucide icons (Check), not text dingbats/emoji.
- **[F]** 11px neutral-600 fine print is borderline low-contrast — Hero and section footnotes (page.tsx:108, 209, 634-638) set #525252-ish text at 11px on near-black — roughly 4:1 contrast at a size where WCAG expects 4.5:1; one shade lighter (neutral-500) would keep…
- **[F]** OG image uses next/og default font, not Geist — Brand typography on the share card doesn't match the site; loading the Geist woff into ImageResponse would tighten consistency at the cost of a font-file read in the route.
- **[F]** Comparison table horizontal scroll on mobile has no affordance — min-w-[640px] table scrolls inside overflow-x-auto but nothing signals scrollability at 390px; an edge fade or 'scroll →' hint would help.
- **[F]** Mobile nav hides 'how it works' and 'compare' anchors entirely — No mobile menu; deep-section navigation on phones relies on scrolling only.
- **[F]** /showdown route exists but is unlinked from the landing page — Built and deployed but unreachable via navigation; either link it (e.g. from the comparison section) or note it as intentionally unlisted.
- **[F]** Red elements smear behind the translucent sticky nav — bg-[#0a0a0a]/85 + backdrop-blur (page.tsx:31) lets the red download/notify buttons bleed through as a blurry red glow behind the logo mid-scroll; raising nav opacity to ~95% would clean it up.
- **[F]** Mobile comparison table has no scroll affordance — At 390px only the FunButton and Handy columns are visible; the only hint that VoiceInk/Wispr exist is cut-off text — a subtle right-edge fade or 'swipe →' hint would help.
- **[F]** Terminal demo is one-shot — fb-line animation plays once per page load (globals.css:66-78); a replay affordance or restart on re-entry would keep the best section alive for users who scroll back.
- **[F]** Footnote text contrast is borderline throughout — font-mono text-[11px] text-neutral-600 on #0a0a0a (e.g. page.tsx:135, 268, 425, 687) sits near 3:1 — intentional de-emphasis, but several of these lines carry substantive claims (pricing caps, GPLv3, …
- **[F]** Star count reintroduction needs a no-tracker mechanism — Critic wants the count back at five digits; doing that without re-adding a third-party request means a build-time GitHub API fetch baked into the static page — worth wiring now so it's a one-line flip…
- **[F]** Terminal demo renders empty in static captures — The IntersectionObserver-gated line stagger means full-page screenshots, print, and reader modes show a blank terminal until scrolled; the noscript fallback covers JS-off but not capture tools — a pre…
- **[F]** Comparison table claims have no verification date — Competitor pricing/capability cells (Wispr $12-15/mo, VoiceInk $25-49) will rot silently; a tiny 'last checked YYYY-MM' footnote keeps the 'honest table' honest over time.
- **[F]** Email success state unreachable without live POST — No dev flag or query param to preview the SuccessState component, so visual QA of that branch requires hitting the real /api/subscribe — a ?preview=success dev-only escape hatch would make design iter…
- **[F]** Mobile nav drops section links entirely — 'how it works' and 'compare' are hidden below sm with no hamburger; acceptable for a single-scroll page but the comparison table — our strongest conversion asset — has no discoverable entry point on p…
- **[F]** Comparison table has no scroll affordance on mobile — At 390px the Handy/VoiceInk/Wispr columns are cut off with only the clipped edge hinting at horizontal scroll (page.tsx:388-389, min-w-[640px]); a subtle fade or 'swipe' hint would help.
- **[F]** Blinking cursor stays on the first typed line after the demo continues — fb-cursor (page.tsx:252) keeps blinking on the git-commit line while a second exchange renders below it — a real terminal would move the cursor; moving the cursor class to the last line (or ending on …
- **[F]** Headless/crawler renders show an empty terminal demo — The IO-gated opacity-0 lines render blank in non-interactive captures (verified in headless Chrome without compositor flags); harmless for humans and the OG image covers unfurlers, but automated scree…
- **[F]** VoiceInk 'API key or account: never' is quibble-able — VoiceInk's prebuilt binary requires a paid license key and its cleanup path uses a cloud provider key; 'never' is defensible for the build-from-source path but a pedantic HN commenter could push back …
- **[F]** Untracked /showdown page exists under apps/web/app but was not judged — apps/web/app/showdown/{page.tsx,data.json} appeared in the worktree (another gauntlet piece per the concurrency note); it builds and routes (/showdown in build output) but is not linked from the landi…
